package heap.mergefiles;

import java.util.PriorityQueue;

/**
 * 1、从每个文件中取出第一个元素放入小顶堆
 * 2、从小顶堆顶取出元素
 * 3、将元素写进大文件中
 * 4、再从该元素所在的小文件中再取一个元素放入小顶堆
 * 5、重复2、3、4步直到所有文件都放入了大文件
 */
public class FilesMerger {



}
